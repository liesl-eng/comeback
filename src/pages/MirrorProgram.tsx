import CategoryPage from "./CategoryPage";

const MirrorProgram = () => (
  <CategoryPage
    category="Mirrors"
    title="Mirrors"
    subtitle="Curated mirrors from premium brands — inspected, in stock, and priced well below retail."
  />
);

export default MirrorProgram;
