import CategoryPage from "./CategoryPage";

const All = () => (
  <CategoryPage
    category="All"
    title="All Products"
    subtitle="Browse the full catalog — filter by brand to narrow down."
  />
);

export default All;
